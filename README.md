# Assginment_Ramcharan_Handling_Messy_Data

# Prefering the purpose of One hot Encoding and Lable Encoding
    -- I prefer One hot encoding for if there are multiple unique values in the column,
    -- I prefer label encoding for only if there two unique values in the column

# Puposes of Scaling and Standardization
    --Both are mainly used in the machine learning process, and they are used to detect the outliers,
    --Scaling is mainly preferred for the image processing or neural networks, and it is used for detecting outliers
    --standardization is used to normalize the values bounding between the -3 to 3 , and it is used to prefer for mainly linear regressions